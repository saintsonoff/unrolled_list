#include <iostream>
#include <vector>
#include <iterator>

#include <unrolled_list.hpp>

int main(int argc, char** argv) {
    // int a;
    // std::cout << typeid(decltype((labwork7::details::Iterator<labwork7::unrolled_list<int>>::reference(a)))).name();

    labwork7::unrolled_list<int>::iterator asffas{};

    // static_assert(std::bidirectional_iterator<labwork7::details::Iterator<labwork7::unrolled_list<int>>>, "alblack");
    labwork7::unrolled_list<int> cont;
    // labwork7::unrolled_list<int>::const_iterator c_itr;
    // labwork7::unrolled_list<int>::iterator itr{c_itr};

    // cont.insert(cont.cend(), 7);

    // labwork7::unrolled_list<int, std::allocator<int>, false> scd;
    // fst.func();
    // std::cout << "\n";
    // scd.func();

    return 0;
}
